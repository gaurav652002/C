#include <stdio.h>
int mathPow(int num, int pow)
{
    int y=num^pow;
        return y;
}
void introduction(char a[20], int b, int p)
{
    printf("my name is %s", a);
    printf("class: %d",b);
    printf("percentage")
}
